import 'package:flutter/material.dart';

class LoginContentMobile extends StatelessWidget {
  const LoginContentMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
